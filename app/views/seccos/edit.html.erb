<h1><%= t('editing') %>  Secco</h1>
<%= form_for(@secco, url:secco_path(@secco, locale: I18n.locale) ) do |f| %>

  <%= render 'form', f:f %>

  <div class="form-group">
    <%= f.submit t('.update') %>
  </div>
  <% end %>

<%= link_to t('show'), curso_secco_path(@curso, @secco, locale:I18n.local) %> |
<%= link_to t('back'), curso_seccos_path(@curso, locale:I18n.locale) %>
